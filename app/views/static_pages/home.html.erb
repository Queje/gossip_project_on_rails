<h1>Bienvenue sur The Gossip Project!</h1>
<p>Messagerie et raggots à gogo pour tous les zozos!</p>

<div>
    <%= link_to "As tu un nouveau potin à raconter?", gossips_new_path %>
</div>

<div class="card-columns">
    <% Gossip.count.times do |i| %>
    
        <div class="card">
            <div class="card-body">
            <h5 class="card-title"><%= link_to "#{Gossip.find(i+1).user.first_name}",author_path(Gossip.find(i+1).id) %></h5>
            <p class="card-text"><%= Gossip.find(i+1).title %></p>
            <%= link_to "voir le potin", gossip_page_path(Gossip.find(i+1).id), class:"card-link" %>
            </div>
        </div>        
    <% end %>
</div>

        
        
